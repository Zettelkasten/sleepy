func main() {
  func count_from_to(Int from, Int to) {
    if from > to { return; }
    print_int(from);
    if from != to { print_char(','); print_char(' '); }
    count_from_to(from + 1, to);
  }

  count_from_to(-3, 5);
  print_char('\n');
}