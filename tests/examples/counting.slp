func main() {
  func count_from_to(from, to) {
    if from > to { return 0; }
    print_double(from);
    if from != to { print_char(','); print_char(' '); }
    count_from_to(from + 1, to);
  }

  count_from_to(-3, 5);
  print_char('\n');
}