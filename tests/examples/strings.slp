func Hello() -> Str {
  @Mutable str = EmptyStr();
  set(str, 0, 'H');
  set(str, 1, 'e');
  set(str, 2, 'l');
  set(str, 3, 'l');
  set(str, 4, 'o');
  return str;
}

func main() {
  hello = Hello();
  print_line(hello);
  free(hello);
}
